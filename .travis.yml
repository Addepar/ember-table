---
language: node_js
node_js:
  # we recommend testing addons with the same minimum supported node version as Ember CLI
  # so that your addon works for all apps
  - '8'

sudo: required
dist: trusty

addons:
  chrome: stable

cache:
  yarn: true

global:
  # See https://git.io/vdao3 for details.
  - JOBS=1
env:
  # we recommend new addons test the current and previous LTS
  # as well as latest stable release (bonus points to beta/canary)
  - RUN_DOCS_TESTS=false EMBER_TRY_SCENARIO=ember-1.12
  - RUN_DOCS_TESTS=false EMBER_TRY_SCENARIO=ember-1.13
  - RUN_DOCS_TESTS=false EMBER_TRY_SCENARIO=ember-lts-2.8
  - RUN_DOCS_TESTS=false EMBER_TRY_SCENARIO=ember-lts-2.18
  - RUN_DOCS_TESTS=false EMBER_TRY_SCENARIO=ember-release
  - RUN_DOCS_TESTS=false EMBER_TRY_SCENARIO=ember-beta
  - RUN_DOCS_TESTS=false EMBER_TRY_SCENARIO=ember-canary
  - RUN_DOCS_TESTS=true EMBER_TRY_SCENARIO=ember-default

matrix:
  fast_finish: true
  allow_failures:
    - env: EMBER_TRY_SCENARIO=ember-canary

before_install:
  - curl -o- -L https://yarnpkg.com/install.sh | bash
  - export PATH=$HOME/.yarn/bin:$PATH

install:
  - yarn install --no-lockfile --non-interactive

script:
  - yarn run lint
  # Usually, it's ok to finish the test scenario without reverting
  #  to the addon's original dependency state, skipping "cleanup".
  - node_modules/.bin/ember try:one $EMBER_TRY_SCENARIO --skip-cleanup
