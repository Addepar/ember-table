{{#each wrappedRows as |api|}}
  {{#if hasBlock}}
    {{yield (hash
      cells=api.cells
      isHeader=api.isHeader

      row=(component "ember-tr" api=api)
    )}}
  {{else}}
    {{ember-tr api=api}}
  {{/if}}
{{/each}}
