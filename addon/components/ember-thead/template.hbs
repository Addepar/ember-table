{{#each wrappedRows as |row index|}}
  {{#if hasBlock}}
    {{yield (hash
      cells=row
      isHeader=true
    )}}
  {{else}}
    {{ember-tr api=(hash
      cells=row
      isHeader=true
    )}}
  {{/if}}
{{/each}}
