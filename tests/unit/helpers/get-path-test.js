
import { getPath } from 'dummy/helpers/get-path';
import { module, test } from 'qunit';

module('Unit | Helper | get path');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getPath([42]);
  assert.ok(result);
});

