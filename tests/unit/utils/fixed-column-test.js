import fixedColumn from 'dummy/utils/fixed-column';
import { module, test } from 'qunit';

module('Unit | Utility | fixed column');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = fixedColumn();
  assert.ok(result);
});
