<div class="demo-container">
  {{#ember-table
    rows=rows
    columns=columns
    tableResizeMode="equal_column"
    customHeaderRowComponent="custom-header"
    onHeaderEvent="onHeaderEvent"
    as |t|
  }}
    {{#ember-table-row
      row=t
      as |cell|
    }}
      {{cell.value}}
    {{/ember-table-row}}
  {{/ember-table}}
</div>
